# コーチ工房の使い方

1. `open-coach-workshop.cmd` をダブルクリック（Node.js 22以上が必要）。ブラウザに工房が開きます。直接ファイルとして開くこともできますが、正本への直接保存はChrome / Edgeのlocalhost利用を推奨します。
2. `coach-generation-prompt.md` を画像生成・ファイル作成ができるAIに渡し、キャラ設定と参考画像を添付します。
3. 工房で `character-package.json` を読み込み、`app_320x448` のPNG9枚をまとめて取り込みます。ZIPは先に展開してください。
4. キャラ・ポーズを選択し、全体倍率またはポーズ別倍率を調整。セリフは3モード×10場面、1行1本で編集できます。
5. **アプリに反映** → `C:\dev\patient-triage` を選択。`coach-library.js` を更新し、直前の内容を日時付きバックアップに保存します。アプリを再読み込みして確認してください。

直接保存できないブラウザでは **反映ファイルをダウンロード** し、正本の `coach-library.js` を置き換えます。途中保存は **編集用JSONを保存** → 後日 **JSONを読み込む**。未反映の作業はブラウザ内だけにあり、自動保存されません。

## 削除・復元

「このキャラを削除」で登場候補と設定一覧から外します。「削除したキャラ」から復元できます。最後の1人は削除できません。既存の画像・dialogue.js・季節パックは書き換えません。

## 反映範囲

- 画像はPNGを1枚2MB以内で取り込み、ライブラリに埋め込んで保持します。追加画像ファイルのコピーは不要です。
- 倍率は吹き出し横のコーチ画像に適用します。ヘッダーは従来の大きさ、タイマー画像は従来の専用画像です。
- 新キャラもヘッダー・各場面・登場キャラ設定へ登録されます。既存キャラのオン/オフ設定は維持します。
- 通常セリフの編集対象は10場面です。季節パック、終了予想、チェック専用、ご褒美の文言は既存の設定を引き続き使用します。
- 二人会話は「二人会話」欄で、既存・追加分をすべて一覧表示します。キャラ・相手・モードで絞り込めます。
- 「会話を編集」で話す二人・登録モード・登場場面・会話パターンを調整できます。会話1つは、名前「セリフ」を1人1行で入力します。「会話の編集を確定」後、「アプリに反映」で保存します。
- 「この会話を削除」はそのまとまりの全パターンを削除します。1パターンだけ消す場合は編集画面で削除してください。「削除した会話も表示」から復元でき、既存会話は「編集前に戻す」でも原文に戻せます。
- 編集・削除は元のdialogue.jsを変更せず、coach-library.jsのbanterEditsへ差分として保存します。GUIからの追加はbanterAddedです。編集用JSONの保存・読み込みでも保持されます。
- 元ファイル側で同じ会話を直接変更すると識別が変わるため、その会話の工房側差分は適用されなくなります。会話の調整は工房に統一してください。
- 既存キャラの画像を差し替えた場合、エストは通常版と色違い版の両方に同じ差し替えが適用されます。
- 公開サイトへは変更ファイルを通常の方法で配信します。今回コミット・pushは実施していません。
- Androidは隣の正本で `npm run cap:sync` を実行してAPKを再ビルド・更新します。今回APK更新は実施していません。
- キャラの定義はアプリの配布ファイルです。タスクデータのバックアップやGAS同期には混ぜません。別PCへの移行時は `coach-library.js` または編集用JSONを移してください。
- service-workerのキャッシュ名は変更していません。最終配信検証時に既存手順に従って更新してください。

## 開発者向け

- `coach-library.js`: 工房が保存するキャラ上書き・追加・削除データ（version 1）。
- `coach-workshop.js`: 形式チェックとアプリへの反映。
- `coach-catalog.js`: 工房用の既存画像一覧。既存画像マップをコード側で変更した場合は合わせて更新してください。
- `coach-editor.html` / `coach-editor.js` / `coach-banter-editor.js`: 工房画面と二人会話エディター。
- 確認: `node coach-workshop.test.cjs` と `node --check prototype_unified_triage.app.js`。
- テスト用ローカルサーバーは読み取り専用、127.0.0.1:4179に限定しています。終了はターミナルでCtrl+C。
